# Data — large and publicly re-derivable; never commit
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata
renv/library/
renv/local/
renv/cellar/
renv/staging/

# Model artifacts
models/*.rds
!models/.gitkeep

# Secrets
.Renviron
*.token

# OS
.DS_Store

# Deployment artifacts — intentionally committed for the Shiny app to serve
!app/model/final_cox_model.rds
!app/model/cohort.rds